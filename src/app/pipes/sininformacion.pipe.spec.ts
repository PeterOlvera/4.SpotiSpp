import { SininformacionPipe } from './sininformacion.pipe';

describe('SininformacionPipe', () => {
  it('create an instance', () => {
    const pipe = new SininformacionPipe();
    expect(pipe).toBeTruthy();
  });
});
